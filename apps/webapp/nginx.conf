user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;

    #gzip  on;

    include /etc/nginx/conf.d/*.conf;

    server {
        listen 3000;
        server_name localhost;

        root /usr/share/nginx/html;  # Replace with the actual path to your Vite build

        location /v1 {
            alias /usr/share/nginx/html;  # Point to the same root directory
            index index.html;
            try_files $uri $uri/ /v1/index.html;
        }

        # Optional: Serve static files directly
        location /v1/assets/ {
            try_files $uri =404;
        }

        # Added: Security headers for all responses
        add_header X-Content-Type-Options nosniff;
        add_header X-Frame-Options DENY;
        add_header X-XSS-Protection "1; mode=block";

        # Optional: Cache static files
        location ~* \.(css|js|jpg|jpeg|gif|png|ico|svg)$ {
            expires 1y;
            add_header Cache-Control "public, must-revalidate, proxy-revalidate";
        }

        # Optional: Gzip compression
        gzip on;
        gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
        gzip_vary on;
    }
}